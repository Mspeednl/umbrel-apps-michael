services:
  web:
    image: ghcr.io/mspeednl/dividend-dashboard:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      DATA_DIR: /data

  app_proxy:
    environment:
      APP_HOST: michael-apps-dividend-dashboard_web_1
      APP_PORT: 8000
